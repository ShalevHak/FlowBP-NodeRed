module.exports = function(RED) {
    function BPIfThenElseNode(n) {
        RED.nodes.createNode(this, n);
        this.condition = n.condition;
        this.name = n.name;
        this.on('input', function(msg) {
        });
    }
    RED.nodes.registerType("if-then-else",BPIfThenElseNode);
}
