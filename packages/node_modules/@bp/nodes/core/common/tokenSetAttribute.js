module.exports = function(RED) {
  function BPSetAttributeNode(n) {
    RED.nodes.createNode(this, n);
    this.attribute=n.attribute;
    this.value=n.value;

    this.on('input', function(msg) {
    });
  }
  RED.nodes.registerType("set-attribute",BPSetAttributeNode);
}
